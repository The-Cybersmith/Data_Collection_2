<h1>Threat#name</h1>
<p>Remember, a minimum of two characters are needed.</p>
<%= form_with( scope: :threat, url: "contact", local: true) do |form| %>
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
